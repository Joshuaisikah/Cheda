<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<!-- SMS: READ_SMS is required to scan the existing inbox (first-run import + rescan).
	     RECEIVE_SMS is required to receive real-time transaction confirmations.
	     Both are dangerous permissions; a clear explanation screen is shown before
	     the system prompt. We never read OTP or marketing SMS (parser filters these).
	     Declared honestly per Google Play SMS permission policy. -->
	<uses-permission android:name="android.permission.READ_SMS" />
	<uses-permission android:name="android.permission.RECEIVE_SMS" />
	<!-- Biometric authentication -->
	<uses-permission android:name="android.permission.USE_BIOMETRIC" />
	<uses-permission android:name="android.permission.USE_FINGERPRINT" />
	<!-- Required on Android 13+ (API 33) to post local notifications -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>